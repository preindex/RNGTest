# RNGTest
 There's a ton of ways to generate random numbers. But which is the best? Most secure? Most efficient?

# Testing
This repo doesn't come with the node_modules to save space. You need to install the dependencies with `npm install` or `bun install`.